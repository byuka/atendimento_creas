<h1 class="h1-padrao"><%= @beneficio.id %> - <%= @beneficio.nome %></h1>
<h4 class="text-center">Informações Gerais</h4>
<ul class="info-list">
    <li><span class="campo">Código:</span> <%= @beneficio.id %></li>
    <li><span class="campo">Nome:</span> <%= @beneficio.nome %></li>
    <li><span class="campo">Descrição:</span> <%= @beneficio.descricao %></li>
    <li><span class="campo">Data de cadastro:</span> <%= l @beneficio.created_at, format: :long %></li>
    <li><span class="campo">Ativo:</span>
        <% if @beneficio.ativo %>
            Sim
        <% else %>
            Não
        <% end %>
    </li>
</ul>
<hr>
<%= link_to  "Voltar", assistances_path %>

